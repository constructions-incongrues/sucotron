#!/bin/bash

docker run \
    -it \
    --init \
    --rm \
    --user $(id -u):$(id -g) \
    -v $PWD/collections:/usr/local/src/sucotron/collections \
    constructionsincongrues/sucotron \
    make "$@"